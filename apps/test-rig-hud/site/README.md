# Hosted console wrapper

This dependency-free build embeds the adjacent standalone dashboard and social
preview into a Cloudflare Worker-compatible ESM entrypoint for private Sites
deployment.

```powershell
npm run build
```
